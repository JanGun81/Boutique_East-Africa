# Kopiera till .env och fyll i värden

# PostgreSQL (t.ex. lokalt, Docker, Supabase, Neon)
DATABASE_URL="postgresql://user:password@localhost:5432/east_african_shop"

# Nästa steg: auth, betalning etc. kan läggas till här
